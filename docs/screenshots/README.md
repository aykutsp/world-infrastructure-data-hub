# Screenshots

Drop the README images here. Current slots referenced from the root `README.md`:

- `trip-istanbul-berlin.png` — Trip mode with the Istanbul → Berlin preset calculated and the route visible on the map.

Capture guidelines:

- Viewport: ~1400 × 900 px
- Dark theme
- PNG, ideally under 1 MB
